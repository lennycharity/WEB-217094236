<%= form_with(model: mark, local: true) do |form| %>
  <% if mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mark.errors.count, "error") %> prohibited this mark from being saved:</h2>

      <ul>
      <% mark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cat %>
    <%= form.number_field :cat, id: :mark_cat %>
  </div>

  <div class="field">
    <%= form.label :regno %>
    <%= form.number_field :regno, id: :mark_regno %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
